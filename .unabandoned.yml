schema: 1

package: "@unabandoned/url"

upstream:
  repo: defunctzombie/node-url
  reason: "unmaintained upstream — the package sits idle while its dependency tree ages"

summary: >-
  Node's core `url` parsing module packaged standalone for use in the browser
  via bundlers like browserify.

why-forked: >-
  A transitive dependency of browserify-style bundling that went unmaintained
  while its tree aged; forked so Renovate keeps that tree current for the
  consumers that pull it in transitively.

status: active

tags: [browser, url, parsing, node-core-polyfill, browserify]
